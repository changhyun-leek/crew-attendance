# 새벽이슬 출석관리 유지보수 가이드

이 문서는 오류 수정과 교사 피드백 반영을 사용자가 직접 의뢰·확인하기 쉽게 만든 안내서입니다.

## 가장 쉬운 수정 흐름

1. 사이트의 `오류·개선 의견 보내기`에서 의견을 접수합니다.
2. 임원교사가 `오류·개선 의견` 메뉴에서 새 의견을 확인하고 `확인 중`으로 바꿉니다.
3. 이 Codex 대화에서 “새벽이슬 출석관리에서 ○○를 수정해줘”라고 요청합니다.
4. 수정된 화면은 프로젝트의 `개발_미리보기_실행.bat`을 더블클릭해 확인합니다.
5. 확인이 끝난 변경만 테스트 후 운영 사이트에 배포합니다.
6. 임원 의견함에서 해당 의견을 `완료`로 바꿉니다.

## 수정 요청에 포함하면 좋은 내용

- 어느 역할인지: 크루교사 / 임원교사 / 보조교사
- 어느 화면인지: 첫 화면 / 출석체크 / 공지 / 학생 관리 등
- 어떤 행동을 했는지
- 기대한 결과와 실제 결과
- 가능하면 휴대폰 기종과 화면 캡처
- 오류가 발생한 날짜와 대략적인 시각

학생의 개인정보, PIN, Supabase 비밀키는 대화나 화면 캡처에 적지 않습니다.

## 프로젝트 위치와 핵심 파일

- 프로젝트 폴더: 현재 이 파일이 있는 폴더
- 화면과 동작: `src/App.tsx`
- 색상·글자·반응형 디자인: `src/styles.css`
- 브라우저와 서버 연결: `src/lib/api.ts`
- 교적부 Excel 생성: `src/lib/registryXlsx.ts`
- 데이터베이스 구조와 보안 규칙: `supabase/migrations/`
- 서버 권한·PIN·보조교사 처리: `supabase/functions/crew-api/`
- 2026 교적부 안전 이관: `scripts/import_2026_roster.py` (개인정보를 출력하거나 파일로 저장하지 않음)
- 자동 배포: `.github/workflows/deploy-pages.yml`
- 운영 배포 순서: `DEPLOYMENT.md`

## 미리보기와 검사

백엔드 없이 화면만 확인하려면 `개발_미리보기_실행.bat`을 더블클릭합니다. 데모 PIN은 `1234`입니다.

코드 검사는 프로젝트 폴더에서 다음 명령을 실행합니다.

```powershell
npm run check
```

테스트와 빌드가 모두 성공해야 운영 배포를 진행합니다.

## 안전한 운영 원칙

- 기존 출석 데이터 백업 없이 데이터베이스 구조를 바꾸지 않습니다.
- `PIN_PEPPER`, VAPID 비밀키, 서비스 권한 키, 초기 관리자 코드는 GitHub에 올리지 않습니다.
- 학생은 삭제하지 않고 장기결석 또는 퇴실로 바꿔 과거 기록을 보존합니다.
- 운영 배포 전 데모 화면과 실제 Supabase 연결 화면을 따로 검사합니다.
- 문제가 생기면 기존 Git 커밋으로 되돌릴 수 있도록 변경 단위마다 커밋합니다.

## 자주 바꾸게 될 항목

- 안내 문구·버튼명: `src/App.tsx`
- 글자 크기·버튼 크기·모바일 배치: `src/styles.css`
- 임시 조사 선택지: 임원 화면의 `공지·임시 항목`에서 코드 수정 없이 생성
- 교사·크루·학생: 임원 화면에서 관리
- 교사 PIN: 교사 본인은 출석 화면의 `내 PIN 변경`, 분실 시에는 임원 화면에서 초기화
- 최초 PIN: 새로 이관된 교사는 본인 이름의 `처음 사용`에서 휴대폰 끝 4자리 확인 후 직접 설정. `ACTIVATION_PEPPER`는 운영 비밀값으로 관리
- Excel 보고: 임원 화면의 `교적부 XLSX`로 기존 12열·크루 블록 형식 다운로드. 민감정보 열은 자동으로 빈칸 유지
- 출석 독려 알림: 교사 기기에서 먼저 `알림 켜기`, 이후 임원 전체 현황에서 미완료 크루에 발송

## 배포 전 체크리스트

- 375px, 390px, 430px 휴대폰 화면에서 가로 넘침이 없는가
- 출석·결석·사유·비고가 저장되는가
- 보조교사가 다른 날짜나 다른 크루에 접근하지 못하는가
- 임원만 전체 데이터와 의견함을 볼 수 있는가
- CSV 한글과 열 순서가 정상인가
- 교적부 XLSX가 Excel에서 열리고 12개 열, 크루 병합, 빈 개인정보 칸이 유지되는가
- 새 교사의 최초 본인 확인, PIN 두 번 입력, 5회 실패 잠금이 정상인가
- 크루별 접기 제목에 출석·결석·미체크·출석률이 맞게 보이는가
- 휴대폰 알림을 켠 교사에게만 독려 알림이 도착하는가
- `npm run check`가 성공하는가
- 기존 출석 건수와 이관 후 건수가 일치하는가
